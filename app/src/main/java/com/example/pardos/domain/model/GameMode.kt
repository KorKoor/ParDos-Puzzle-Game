package com.example.pardos.domain.model

/**enum class GameMode {
    /** Rush de niveles, desde 1 hasta X. No se guarda el progreso al cerrar. */
    CARRERA,

    /** Modo normal con progresión persistente. Se guarda en la DB. */
    LIBRE,

    Zen,
    /** Niveles aleatorios con límite de tiempo. */
    DESAFIO
}**/